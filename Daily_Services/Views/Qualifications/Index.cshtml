@model IEnumerable<Daily_Services.Models.Qualification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Qualification_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Qualification_Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Qualification_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Qualification_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Qualification_Id })
        </td>
    </tr>
}

</table>
